// Funciones para completacion de datos en carga de borrador

// Cargar tipo de servicio en dropdown menu
$('[name=tipo]').val(tipoS);

// Cargar fecha de creacion de servicio
$('[name=fechaCreacion]').val(fechaCreacionS);

// Cargar hora de creacion de servicio
$('[name=horaCreacion]').val(horaCreacionS);

// Cargar fecha de finalizacion de servicio
$('[name=fechaFinalizacion]').val(fechaFinalizacionS);

// Cargar hora de finalizacion de servicio
$('[name=horaFinalizacion]').val(horaFinalizacionS);